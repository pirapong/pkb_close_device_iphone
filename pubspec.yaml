name: pkb_close_device_iphone
description: A new Flutter package project.
version: 1.0.0
homepage: https://github.com/pirapong/pkb_close_device_iphone.git

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  device_info_plus: ^9.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: